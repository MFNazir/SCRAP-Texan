<%= form_with(model: employee_status, local: true) do |form| %>
  <% if employee_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_status.errors.count, "error") %> prohibited this employee_status from being saved:</h2>

      <ul>
        <% employee_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="album text-white">
  
          <div class="row">

              <div class="col-md-4 text-white">

                  <div class="form-group">
                    <%= form.label :employee_status, "Employee Status" %>
                    <%= form.text_field :employee_status, class: 'form-control' %>
                  </div>

                  <div class="form-group">
                    <%= form.label :employee_status_desc, "Employee Status Description" %>
                    <%= form.text_field :employee_status_desc, class: 'form-control' %>
                  </div>

                </div>

          </div>  

          <hr class="new1">

              <div class="col-sm-12">
                <div class="form-group text-white">

                  <div class="actions">
                    <%= form.submit "Add Employee Status", class: 'btn btn-secondary btn-block' %>
                  </div>

                  </div>
                </div> 


<% end %>
