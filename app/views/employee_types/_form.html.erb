<%= form_with(model: employee_type, local: true) do |form| %>
  <% if employee_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_type.errors.count, "error") %> prohibited this employee_type from being saved:</h2>

      <ul>
        <% employee_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="album text-white">
  
          <div class="row">

              <div class="col-md-4 text-white">

              <div class="form-group">
                <%= form.label :employee_type, "Employee Type" %>
                <%= form.text_field :employee_type, class: 'form-control' %>
              </div>

              <div class="form-group">
                <%= form.label :employee_type_desc, "Employee Type Description" %>
                <%= form.text_field :employee_type_desc, class: 'form-control' %>
              </div>

              </div>

          </div>

          <hr class="new1">

              <div class="col-sm-12">
                <div class="form-group text-white">  

                    <div class="actions">
                      <%= form.submit "Add Employee Type", class: 'btn btn-secondary btn-block' %>
                    </div>
                  </div>
                </div>    
<% end %>
