<%= form_with(model: invoice_status, local: true) do |form| %>
  <% if invoice_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_status.errors.count, "error") %> prohibited this invoice_status from being saved:</h2>

      <ul>
        <% invoice_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="album text-white">
  
          <div class="row">

              <div class="col-md-4 text-white">
                <div class="form-group">
                  <%= form.label :invoice_status, "Invoice Status:" %><i style=" color:aquamarine;"> *</i>
                  <%= form.text_field :invoice_status, placeholder: "Enter Status Here", class: 'form-control' %>
                </div>

                <div class="form-group">
                  <%= form.label :invoice_status_desc, "Invoice Status Description:" %><i style=" color:aquamarine;"> *</i>
                  <%= form.text_field :invoice_status_desc, placeholder: "Enter A Description Here", class: 'form-control' %>
                </div>

                </div>

             </div> 

             <hr class="new1">
             <div class="col-sm-12">
                <div class="form-group text-white">

                <div class="actions">
                  <%= form.submit "Add Invoice Status", class: 'btn btn-secondary btn-block' %>
                </div>
              </div>
             </div> 
<% end %>
