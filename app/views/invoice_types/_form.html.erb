<%= form_with(model: invoice_type, local: true) do |form| %>
  <% if invoice_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_type.errors.count, "error") %> prohibited this invoice_type from being saved:</h2>

      <ul>
        <% invoice_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="album text-white">
  
          <div class="row">

              <div class="col-md-5 text-white">

                <div class="form-group">
                  <%= form.label :invoice_type, "Invoice Type" %>
                  <%= form.text_field :invoice_type, placeholder: "Enter Invoice Type Here",class: 'form-control' %>
                </div>

                <div class="form-group">
                  <%= form.label :invoice_type_desc, "Invoice Type Description" %>
                  <%= form.text_field :invoice_type_desc, placeholder: "Enter A Small Description Here",class: 'form-control' %>
                </div>

                </div>

             </div> 

             <hr class="new1">

              <div class="col-sm-12">
                <div class="form-group text-white">

                    <div class="actions">
                      <%= form.submit "Save Invoice Type", class: 'btn btn-secondary btn-block' %>
                    </div>
                    </div>
                </div> 
<% end %>
