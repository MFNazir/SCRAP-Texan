<%= form_with(model: invoice_type, local: true) do |form| %>
  <% if invoice_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_type.errors.count, "error") %> prohibited this invoice_type from being saved:</h2>

      <ul>
        <% invoice_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :invoice_type %>
    <%= form.text_field :invoice_type %>
  </div>

  <div class="field">
    <%= form.label :invoice_type_desc %>
    <%= form.text_field :invoice_type_desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
