<%= form_with(model: vehicle_status, local: true) do |form| %>
  <% if vehicle_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_status.errors.count, "error") %> prohibited this vehicle_status from being saved:</h2>

      <ul>
        <% vehicle_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
   <div class="album text-white">
  
          <div class="row">
            <div class="col-md-4 text-white">


              <div class="form-group">
                <%= form.label :vehicle_status, "Vehicle Status" %>
                <%= form.text_field :vehicle_status, class: 'form-control' %>
              </div>

              <div class="form-group">
                <%= form.label :vehicle_status_desc, "Vehicle Status Description" %>
                <%= form.text_field :vehicle_status_desc, class: 'form-control' %>
              </div>
              </div>

             </div> 
             <hr class="new1">
             <div class="col-sm-12">
                <div class="form-group text-white">

                    <div class="actions">
                      <%= form.submit "Add Vehicle Status", class: 'btn btn-secondary btn-block' %>
                    </div>
                    </div>
             </div> 
<% end %>
